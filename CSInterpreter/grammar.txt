expr: term ( (PLUS|MINUS) term )*
term: factor( (MUL|DIV) factor)*
factor: INTEGER |(LPAREN expre RPAREN)

expr: operand (operator)*
operator: unaOperator | (binOperator operand)

endpoint: #STRING | (LPAREN expre RPAREN)