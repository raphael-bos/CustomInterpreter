namespace CSInterpreter
{
    enum TokenType
    {
        INTEGER,
        PLUS,
        MINUS,
        MUL,
        DIV,
        LPAREN,
        RPAREN,
        STRING,
        ENDPOINT,
        FREQ,
        AND,
        OR,
        INTERVAL,
        TTI,
        EOF
    }
}